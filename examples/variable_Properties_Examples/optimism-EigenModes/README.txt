*** optimism-directFRF/Test1 README ***

This example calculates n modes of a given geometry, for both a loaded and unloaded configuration. User can specify the geometry, number of nodes, fixed B.C., and the magnitude of the applied load. 
